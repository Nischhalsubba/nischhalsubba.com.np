# System runtime features

Browser modules whose primary responsibility belongs to the **system** domain. Keep cross-domain orchestration in `src/scripts/entrypoints/` and shared primitives in `src/scripts/shared/`.
