{
  "name": "nischhal-raj-subba-portfolio",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build && node scripts/copy-static-assets.cjs && node scripts/generate-resume-pdf.cjs && node scripts/audit-build.cjs",
    "preview": "vite preview",
    "preview:static": "npx http-server -p 4173 .",
    "check:links": "node scripts/check-links.js",
    "audit:build": "node scripts/audit-build.cjs"
  },
  "devDependencies": {
    "@types/node": "^22.14.0",
    "typescript": "~5.8.2",
    "vite": "^6.2.0"
  }
}
