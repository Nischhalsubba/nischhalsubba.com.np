{
  "name": "nischhal-portfolio-cloudflare-bootstrap",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "npm --prefix site ci && npm --prefix site run build",
    "validate": "npm --prefix site ci && npm --prefix site run validate",
    "preview": "npm --prefix site run preview --"
  }
}
